# Dev container with Maven + JDK and Tesseract for hot-reload workflows
FROM maven:3.9-eclipse-temurin-21

RUN apt-get update && \
    apt-get install -y tesseract-ocr tesseract-ocr-eng tesseract-ocr-deu && \
    apt-get clean && \
    rm -rf /var/lib/apt/lists/*

WORKDIR /workspace
